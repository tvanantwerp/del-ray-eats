{
  "name": "@example/minimal",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "npm run build:images && astro dev",
    "build": "npm run build:*",
    "build:astro": "astro build",
    "build:images": "node ./scripts/imagesProcessing.js",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^0.22.9",
    "fs-extra": "^10.0.0",
    "sharp": "^0.29.3"
  }
}
