---
const {href, external} = Astro.props
---

<a
  class="bg-red-600 block cursor-pointer font-semibold font-sans px-4 py-2 rounded-2xl text-center text-lg text-white"
  href={href}
  rel={external ? "noopener noreferrer" : undefined}
  target={external ? "_blank" : undefined}>
  <slot />
</a>